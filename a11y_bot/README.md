# Accessibility Review Agent

Very small hackathon app that reviews Markdown for accessibility issues using OpenAI and shows results in Streamlit (score, issues, summary, JSON export).

## Install
1. `pip install -r requirements.txt`
2. `export OPENAI_API_KEY=your_key_here`

## Run
1. `streamlit run app.py`
